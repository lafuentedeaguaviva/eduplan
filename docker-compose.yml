version: '3.8'
services:
  eduplan-pro:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: eduplan-pro
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # - NEXT_PUBLIC_SUPABASE_URL=YOUR_SUPABASE_URL
      # - NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
